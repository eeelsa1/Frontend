const friends = ["Harry", "Ron", "Snap", "Mike", "Grace"];
const reversed_friends = [];
for (let i = friends.length; i > 0; i--) {
  //take = Array.prototype.pop(friends);
  let reversed_friends = friends.push[i];
}
